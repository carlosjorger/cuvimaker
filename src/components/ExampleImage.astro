---
const { imageSrc = '', height = 1200, width = 1100 } = Astro.props;
---

<picture>
	<img
		class="bg-transparent"
		src={imageSrc}
		alt="logo"
		width="400"
		class:list={[`aspect-[${height}/${width}]`]}
	/>
</picture>
