---
const { imageSrc = '', width = 350 } = Astro.props;
---
<picture class="block" class:list={[`max-w-[${width}px]`]}>
	<img
		class="bg-transparent"
		src={imageSrc}
		alt="logo"
		width={width}
	/>
</picture>
